{
    "type": "project",
    "license": "proprietary",
    "minimum-stability": "dev",
    "prefer-stable": true,
    "require": {
        "php": ">=7.2.5",
        "ext-ctype": "*",
        "ext-iconv": "*",
        "composer/package-versions-deprecated": "1.11.99.1",
        "doctrine/doctrine-bundle": "^2.2",
        "doctrine/doctrine-migrations-bundle": "^3.0",
        "doctrine/orm": "^2.8",
        "nelmio/cors-bundle": "^2.1",
        "sensio/framework-extra-bundle": "^6.1",
        "symfony/console": "5.2.*",
        "symfony/dotenv": "5.2.*",
        "symfony/flex": "^1.3.1",
        "symfony/framework-bundle": "5.2.*",
        "symfony/property-access": "5.2.*",
        "symfony/proxy-manager-bridge": "5.2.*",
        "symfony/serializer": "5.2.*",
        "symfony/yaml": "5.2.*"
    },
    "require-dev": {
        "doctrine/doctrine-fixtures-bundle": "^3.4",
        "fzaninotto/faker": "^1.9",
        "symfony/maker-bundle": "^1.29"
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "App\\Tests\\": "tests/"
        }
    },
    "replace": {
        "symfony/polyfill-ctype": "*",
        "symfony/polyfill-iconv": "*",
        "symfony/polyfill-php72": "*"
    },
    "scripts": {
        "server": [
            "php -S localhost:8000 -t public"
        ],
        "lint": [
            "phpcs --standard=PSR1,PSR2 src"
        ],
        "db:init": [
            "docker run --rm --name mysql -e MYSQL_ROOT_PASSWORD='root' -e MYSQL_DATABASE='symfony' -e MYSQL_USER='symfony' -e MYSQL_PASSWORD='symfony' -d -p 3306:3306 mysql --default-authentication-plugin=mysql_native_password"
        ],
        "db:vinit": [
            "docker run --rm --name mysql -v $(pwd)/mnt-data:/var/lib/mysql -e MYSQL_ROOT_PASSWORD='root' -e MYSQL_DATABASE='symfony' -e MYSQL_USER='symfony' -e MYSQL_PASSWORD='symfony' -d -p 3306:3306 mysql --default-authentication-plugin=mysql_native_password"
        ],
        "auto-scripts": {
            "cache:clear": "symfony-cmd",
            "assets:install %PUBLIC_DIR%": "symfony-cmd"
        },
        "post-install-cmd": [
            "@auto-scripts"
        ],
        "post-update-cmd": [
            "@auto-scripts"
        ]
    },
    "conflict": {
        "symfony/symfony": "*"
    },
    "extra": {
        "symfony": {
            "allow-contrib": false,
            "require": "5.2.*"
        }
    }
}
